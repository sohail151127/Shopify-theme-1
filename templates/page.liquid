{% include 'breadcrumb' %}

<section class="section-b-space">
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        {{ page.content }}
      </div>
    </div>
  </div>
</section>
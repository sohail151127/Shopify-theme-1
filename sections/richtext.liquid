<!-- about section-->
{% if section.settings.enabled %}
{% capture sectionId %}{{ section.id }}{% endcapture %}
<section>
  <div class="container">
    <div class="row">
      <div class="col-lg-8 offset-lg-2">

        {% if settings.title_layout == 'title_1' %}
        <div class="title1">
          {% if section.settings.subtitle != blank %}
          <h4 {% if settings.enable_lang_switcher %}class="lang_trans" data-trans="#richtext_{{sectionId}}_subtitle"{% endif %}>
            {{ section.settings.subtitle }}
          </h4>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_subtitle">{{ section.settings.trans_subtitle | raw }}</span>
          {% endif %}
          {% endif %}
          {% if section.settings.title != blank %}
          <h2 class="title-inner1 {% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_title"{% endif %}>
            {{ section.settings.title }}
          </h2>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_title">{{ section.settings.trans_title | raw }}</span>
          {% endif %}
          {% endif %}
        </div>
        {% elsif settings.title_layout == 'title_2' %}
        <div class="title3">
          {% if section.settings.subtitle != blank %}
          <h4 {% if settings.enable_lang_switcher %}class="lang_trans" data-trans="#richtext_{{sectionId}}_subtitle"{% endif %}>
            {{ section.settings.subtitle }}
          </h4>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_subtitle">{{ section.settings.trans_subtitle | raw }}</span>
          {% endif %} 
          {% endif %}
          {% if section.settings.title != blank %}
          <h2 class="title-inner3 {% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_title"{% endif %}>
            {{ section.settings.title }}
          </h2>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_title">{{ section.settings.trans_title | raw }}</span>
          {% endif %} 
          {% endif %}
          <div class="line"></div>
        </div>
        {% elsif settings.title_layout == 'title_3' %}
        <div class="title2">
          {% if section.settings.subtitle != blank %}
          <h4 {% if settings.enable_lang_switcher %}class="lang_trans" data-trans="#richtext_{{sectionId}}_subtitle"{% endif %}>
            {{ section.settings.subtitle }}
          </h4>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_subtitle">{{ section.settings.trans_subtitle | raw }}</span>
          {% endif %}
          {% endif %}
          {% if section.settings.title != blank %}
          <h2 class="title-inner2 {% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_title"{% endif %}>
            {{ section.settings.title }}
          </h2>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_title">{{ section.settings.trans_title | raw }}</span>
          {% endif %}
          {% endif %}
        </div>
        {% elsif settings.title_layout == 'title_4' %}
        <div class="title4">
          {% if section.settings.subtitle != blank %}
          <h4 {% if settings.enable_lang_switcher %}class="lang_trans" data-trans="#richtext_{{sectionId}}_subtitle"{% endif %}>
            {{ section.settings.subtitle }}
          </h4>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_subtitle">{{ section.settings.trans_subtitle | raw }}</span>
          {% endif %}
          {% endif %}
          {% if section.settings.title != blank %}
          <h2 class="title-inner4 {% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_title"{% endif %}>
            {{ section.settings.title }}
          </h2>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_title">{{ section.settings.trans_title | raw }}</span>
          {% endif %}
          {% endif %}
          <div class="line">
            <span></span>
          </div>
        </div>
        {% elsif settings.title_layout == 'title_5' %}
        <div class="title1 title-gradient ">
          {% if section.settings.subtitle != blank %}
          <h4 {% if settings.enable_lang_switcher %}class="lang_trans" data-trans="#richtext_{{sectionId}}_subtitle"{% endif %}>
            {{ section.settings.subtitle }}
          </h4>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_subtitle">{{ section.settings.trans_subtitle | raw }}</span>
          {% endif %}          
          {% endif %}
          {% if section.settings.title != blank %}
          <h2 class="title-inner1 {% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_title"{% endif %}>
            {{ section.settings.title }}
          </h2>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_title">{{ section.settings.trans_title | raw }}</span>
          {% endif %}  
          {% endif %}
        </div>
        {% elsif settings.title_layout == 'title_6' %}
        <div class="title1 title5">
          {% if section.settings.subtitle != blank %}
          <h4 {% if settings.enable_lang_switcher %}class="lang_trans" data-trans="#richtext_{{sectionId}}_subtitle"{% endif %}>
            {{ section.settings.subtitle }}
          </h4>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_subtitle">{{ section.settings.trans_subtitle | raw }}</span>
          {% endif %}          
          {% endif %}
          {% if section.settings.title != blank %}
          <h2 class="title-inner1 {% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_title"{% endif %}>
            {{ section.settings.title }}
          </h2>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_title">{{ section.settings.trans_title | raw }}</span>
          {% endif %}  
          {% endif %}
          <hr role="tournament6">
        </div>
        {% endif %}

        <div class="about-text">
          <p class="{% if settings.enable_lang_switcher %}lang_trans{% endif %}" {% if settings.enable_lang_switcher %}data-trans="#richtext_{{sectionId}}_desc"{% endif %}>
            {{ section.settings.desc }}
          </p>
          {% if settings.enable_lang_switcher %}
          <span class="hide" id="richtext_{{sectionId}}_desc">{{ section.settings.trans_desc | raw }}</span>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</section>
{% endif %}
<!-- about section end -->

{% schema %}
{
   "name":"Richtext",
   "settings":[
      {
         "type":"checkbox",
         "id":"enabled",
         "default":true,
         "label":"Enabled"
      },
      {
         "type":"text",
         "id":"title",
         "label":"Heading",
		 "default": "Welcome to multistore"
      },
      {
         "type":"textarea",
         "id":"trans_title",
         "label":"Translate Title"
      },
      {
         "type":"text",
         "id":"subtitle",
         "label":"Sub title",
		 "default": "About Us"
      },
      {
         "type":"textarea",
         "id":"trans_subtitle",
         "label":"Translate Subtitle"
      },
      {
         "type":"text",
         "id":"desc",
         "label":"Description",
		 "default": "Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi architecto beatae vitae dicta sunt explicabo."
      },
      {
         "type":"textarea",
         "id":"trans_desc",
         "label":"Translate desc"
      }
   ],
   "presets":[
      {
         "name":"Richtext",
         "category":"text",
         "settings":{ }
      }
   ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
